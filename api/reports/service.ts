import { CreateReportInput, CreateReportReponse } from './dto';

export const create = async (input: CreateReportInput): Promise<CreateReportReponse> => {
  //TODO: Logc to create a report
  return {
    id: '123',
  };
};
